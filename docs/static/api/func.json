{
  "namespace": "func",
  "description": "Function inspection and compilation utilities.",
  "functions": [
    {
      "name": "compile",
      "description": "Compiles a Lua string into a function without executing it. The chunk is loaded under the chunkname `@lje_dynamic_compile` and flagged internally as an LJE proto. Returns `nil` on compilation failure (the error is printed to the console).",
      "params": [
        {
          "name": "source",
          "type": "string",
          "description": "Lua source code to compile."
        }
      ],
      "returns": [
        {
          "type": "function | nil",
          "description": "The compiled function, or `nil` if compilation failed."
        }
      ]
    },
    {
      "name": "type",
      "description": [
        "Returns the internal LuaJIT fast function ID (`ffid`) of a function.",
        "",
        "- `0` means a Lua function",
        "- `1` means a C function",
        "- Anything else is the unique ID of a fast function."
      ],
      "params": [
        {
          "name": "fn",
          "type": "function",
          "description": "The function to inspect."
        }
      ],
      "returns": [
        {
          "type": "number",
          "description": "The `ffid` value. `0` for regular Lua functions."
        }
      ]
    }
  ]
}
