{
  "namespace": "base",
  "description": "Lua-defined extensions set up by the secure preinit script. The preinit also pulls a curated subset of the GMod API into the secure state's global environment via lje.secure.pull (e.g. ents, render, surface, timer, util, Vector, LocalPlayer, ...), registers class metatables in the registry, and installs a few polyfills.",
  "constants": [
    {
      "name": "lje.includeCache",
      "type": "table",
      "description": "A per-script cache used by `lje.require`. Keyed by script name, then by path. Populated automatically — you generally do not need to interact with this directly."
    },
    {
      "name": "hook",
      "type": "table",
      "description": "A minimal replacement for GMod's hook library, defined in the secure state. `hook.Add(event, identifier, func)` registers a listener; `hook.Listen()` installs an engine call hook (via `lje.vm.set_engine_call_hook`) that dispatches engine events to the registered listeners; `hook.Call` is a no-op. Listener return values are not supported yet."
    },
    {
      "name": "cam.Start2D",
      "type": "function",
      "description": "A polyfill for GMod's `cam.Start2D`, implemented on top of the pulled `cam.Start` function."
    }
  ],
  "functions": [
    {
      "name": "require",
      "defined_in": "lua",
      "description": "A cached version of `lje.include`. The first call for a given `path` within a script context executes the file and caches the result. Subsequent calls return the cached value immediately. Must be called within an active script context.",
      "params": [
        {
          "name": "path",
          "type": "string",
          "description": "Path to the file to include, relative to the current script."
        }
      ],
      "returns": [
        {
          "type": "any",
          "description": "The return value of the included file, or the cached result from a prior call."
        }
      ]
    },
    {
      "name": "con_printf",
      "defined_in": "lua",
      "description": "A `string.format`-style console print with inline ANSI color support.\n\n- `$name{...}` sets the **foreground**.\n- `$$name{...}` sets the **background**.\n\nBlocks nest, and closing one restores whatever was active around it, so you can combine layers: `$$blue{$white{text}}`.\n\n**Color names**\n\n- Base 8: `black`, `red`, `green`, `yellow`, `blue`, `magenta`, `cyan`, `white`, plus `default`.\n- Bright variants: prefix with `bright` or `light` (`brightred`, `lightblue`, ...). `gray`/`grey` is the bright-black slot.\n- Extended: `orange`, `pink`, `hotpink`, `purple`, `violet`, `indigo`, `lime`, `mint`, `teal`, `turquoise`, `navy`, `maroon`, `olive`, `brown`, `gold`, `tan`, `salmon`, `coral`, `lavender`, `silver`, `slate`, `charcoal`, `cream`, `darkgreen`, `darkblue`, `darkred`.\n- Hex truecolor: `$#ff8800{...}` or the short form `$#f80{...}`.\n- Raw xterm-256 index: `$196{...}`, `$$21{...}`.\n\n**Styles** (foreground syntax only): `bold`, `dim`, `italic`, `underline`, `blink`, `reverse`/`invert`, `hidden`, `strike`.\n\nAn unrecognized name is left in the output as-is rather than being swallowed, so typos are visible.\n\nExample:\n```lua\nlje.con_printf(\"$red{Error}: %s\", message)\nlje.con_printf(\"$$red{$white{ FATAL }} $gray{disk on fire}\")\nlje.con_printf(\"$#ff8800{warning} on $$#202020{a dark strip}\")\n```",
      "params": [
        {
          "name": "fmt",
          "type": "string",
          "description": "A `string.format` format string. Color codes use the syntax `$colorName{text}` for the foreground and `$$colorName{text}` for the background."
        },
        {
          "name": "...",
          "type": "any",
          "optional": true,
          "description": "Format arguments, passed to `string.format`."
        }
      ],
      "returns": []
    }
  ]
}
