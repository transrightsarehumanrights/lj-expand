{
  "namespace": "env",
  "description": "Environment management, script context inspection, and global state control.",
  "functions": [
    {
      "name": "current_script",
      "description": "Returns the name of the currently executing LJE script, if any.",
      "params": [],
      "returns": [
        {
          "type": "string | nil",
          "description": "The script name, or `nil` if no script is currently active."
        }
      ]
    },
    {
      "name": "current_script_path",
      "description": "Returns the full path of the currently executing LJE script, if any.",
      "params": [],
      "returns": [
        {
          "type": "string | nil",
          "description": "The script path, or `nil` if no script is currently active."
        }
      ]
    },
    {
      "name": "find_script_files",
      "description": [
        "Searches for files matching `search_path` relative to the current script's directory. Returns `nil` if no script is active.",
        "",
        "Supports wildcard patterns, so if you wanted to find all files in `detours/`, you would do `detours/*`."
      ],
      "params": [
        {
          "name": "search_path",
          "type": "string",
          "description": "A path pattern to search for, relative to the current script."
        }
      ],
      "returns": [
        {
          "type": "table | nil",
          "description": "An array of matching file path strings, or `nil` if no script is active."
        }
      ]
    },
    {
      "name": "read_script_file",
      "description": [
        "Reads the contents of a file relative to the current script's directory. Returns `nil` if no script is active or the file cannot be read."
      ],
      "params": [
        {
          "name": "relative_path",
          "type": "string",
          "description": "The path to the file to read, relative to the current script."
        }
      ],
      "returns": [
        {
          "type": "string | nil",
          "description": "The file contents as a string, or `nil` if no script is active or the file cannot be read. Binary files work as well."
        }
      ]
    },
    {
      "name": "is_lua_involved",
      "description": "Returns whether a Lua function is present in the call stack at or above the given frame offset.",
      "params": [
        {
          "name": "frame_offset",
          "type": "integer",
          "optional": true,
          "description": "The frame level to start checking from. Defaults to `1`."
        }
      ],
      "returns": [
        {
          "type": "boolean",
          "description": "`true` if a Lua frame is involved at or above the offset."
        }
      ]
    },
    {
      "name": "is_lje_involved",
      "description": "Returns whether an LJE frame is present in the call stack, starting from `frame_offset` and searching up to `max_depth` frames.",
      "params": [
        {
          "name": "frame_offset",
          "type": "integer",
          "optional": true,
          "description": "The frame level to start checking from. Defaults to `1`."
        },
        {
          "name": "max_depth",
          "type": "integer",
          "optional": true,
          "description": "Maximum number of frames to search. Defaults to `-1` (unlimited)."
        }
      ],
      "returns": [
        {
          "type": "boolean",
          "description": "`true` if an LJE frame is found within the search range."
        }
      ]
    },
    {
      "name": "is_lje_frame",
      "description": "Returns whether the frame at exactly `frame_offset` is an LJE frame.",
      "params": [
        {
          "name": "frame_offset",
          "type": "integer",
          "optional": true,
          "description": "The frame level to check. Defaults to `1`."
        }
      ],
      "returns": [
        {
          "type": "boolean",
          "description": "`true` if the specified frame is an LJE frame."
        }
      ]
    },
    {
      "name": "show_special_frames",
      "description": "Controls whether special (internal LJE) frames are visible in stack introspection. Useful when debugging inter-LJE interactions.",
      "params": [
        {
          "name": "show",
          "type": "boolean",
          "description": "`true` to make special frames visible, `false` to hide them."
        }
      ],
      "returns": []
    },
    {
      "name": "start_timing",
      "description": "Begins timing a code section to hide its execution time from other scripts. Must be paired with `lje.env.stop_timing` to take effect.",
      "params": [],
      "returns": []
    },
    {
      "name": "end_timing",
      "description": "Ends a timing section started by `lje.env.start_timing`, adding to the LJE clock deficit counter. All high-resolution timers will be adjusted by the total deficit to hide the time spent in the timed section from other scripts.",
      "params": [],
      "returns": []
    },
    {
      "name": "clock_deficit",
      "description": "Returns the current total clock deficit accumulated by `lje.env.start_timing` / `lje.env.end_timing` pairs. This value is reduced over time through precise spinwaits to recuperate the lost time.",
      "params": [],
      "returns": [
        {
          "name": "deficit",
          "type": "number",
          "description": "The current clock deficit in seconds."
        }
      ]
    }
  ]
}
